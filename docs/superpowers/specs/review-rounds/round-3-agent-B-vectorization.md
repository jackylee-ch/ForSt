# Round 3 — Reviewer B (Vectorization), NEW findings

Scope: rarely-reviewed paths + Round-2 fix per-row code + JMH benchmarks.
Files under audit live in sibling repo `/Users/lijunqing/Code/stczwd/flink/flink-state-backends/flink-statebackend-forst-rs/`.

HIGH count: **6**

- **B3-H1** `state/ForStRsAsyncReducingStateV2.java:97,134` (and `:Aggregating:100,138`) — `serializeKey/serializeValue` (scalar path) calls `getCopyOfBuffer()` → per-event byte[] allocation; doc claims an "RMW cache + flushOnBarrier", but the class contains neither — async-V2 reduce/aggregate has zero caching layer.
- **B3-H2** `state/ForStRsAsyncReducingStateV2.java:183-196` (and Aggregating) — `buildDBGetRequest`/`buildDBPutRequest` allocate two fresh byte[]s per request and a new `ForStRsDBGetRequest`/`ForStRsDBPutRequest` wrapper object; never routed through the vectorized `ColumnarBatchBuffer.append` path that exists in the same class.
- **B3-H3** `keyed/ForStRsKeyGroupedSerializer.java:79,242` — `encodeForState`/`encodeForMap` re-encode `stateName.getBytes(UTF_8)` (new byte[]) on every single call; the off-heap variants already cache `preEncodedStateNameBytes`, but the heap variants are still called by every V1 ValueState `computeKey()` path and by all four V2 inner-tables above.
- **B3-H4** `state/ForStRsValueState.java:362-364,422-423` — V1-sync `update()` / `getAndUpdate()` (the "reference impl") still calls `outputBuffer.getCopyOfBuffer()` even after a hot-path `computeKey()` already returned a fresh byte[]; per-event two heap allocations on the path Q11 is pinned to.
- **B3-H5** `cache/MapStateCache.java:91,108,113` — every `lookup/put/remove` allocates a new `BytesKey` wrapper (and computes `Arrays.hashCode` over the full composite key) on EACH call; the evict path inside `LinkedHashMap` accessOrder=true also relinks nodes on every `get` — turning every cache HIT into a node-pointer write and an allocation. H8 only flagged the wrapper; the access-order relink overhead on hit is the new finding.
- **B3-H6** `VectorizedExecutor.java:238` (`executeRequestSync`) — allocates a brand-new `VectorizedClassifier` + iterator over `ConcurrentHashMap.KeySetView` PER incoming sync request, then calls `initNewKindBuffers(arena)` (Arena allocation) for a single-row path. Per-row future-check loop at `:194-213` / `:256-272` additionally instantiates `RuntimeException("...cause unavailable")` on every exceptional row (allocation in a per-row error loop, even if rare).

JMH benchmark audit (`src/test/java/.../jmh/`): **the three benches do not exercise any vectorized hot path.** `ForStRsBProdBenchmark` measures `dbSnapshot` latency + single-CF `lookupKv`/`put`; `ForStRsFfmBenchmark` measures scalar `put`/`lookupKv` and raw `batchPut`/`batchPutArrow`; `ForStCompareBenchmark` is comment-only ("could be wrapped in `@Benchmark`-annotated stubs"). NONE call `vectorizedBatchPut`/`vectorizedBatchGet`, `VectorizedExecutor.executeBatchRequests`, `MapStateCache`, `ForStRsAsyncReducingStateV2/AggregatingStateV2`, the V2 async dispatcher, or any encode-into-`ColumnarBatchBuffer` path. The "B-Prod acceptance" bench's own header (line 55-62) admits the module has no JMH plugin wiring — they are plain `@Test`s tagged `bench`. This means the perf claims about vectorization speed-ups have no in-tree JMH evidence.
